# TESTS:
# misaligned dates causes fail
# misaligned column names causes fail
# trade buffer position size calcs are correct - check several examples. What edge cases?
# commission calcs correct - edge cases?
# test when sum(abs(weights)) are greater than 1

test_that("what to test", {
  expect_
})
